{
  "name": "poke-voice",
  "version": "0.1.0",
  "description": "ElevenLabs MCP server for Poke, deployable to Vercel.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "typecheck": "tsc --noEmit",
    "test-client": "node scripts/test-client.mjs"
  },
  "dependencies": {
    "mcp-handler": "^1.0.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=20"
  }
}
