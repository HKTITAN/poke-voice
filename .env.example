# Optional: fallback ElevenLabs API key if the MCP client doesn't send one via Authorization header.
# Leave unset for BYO-key deployments.
ELEVENLABS_API_KEY=

# Optional: default voice ID used when a tool doesn't specify one.
ELEVENLABS_DEFAULT_VOICE_ID=cgSgspJ2msm6clMCkdW9
